import {
    Container,
} from './styles'
import { useState } from 'react';

export default function InputSearch({ title }) {

    return (
        <Container>
            <h4>{title}</h4>
        </Container>

    )
}