import {
    Container,
} from './styles';

export default function QrCode({ }) {
    // const [isMenu, setIsMenu] = useState(false);

    return (
        <Container>
            QrCode
        </Container>
    )
}