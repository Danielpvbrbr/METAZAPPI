import {
    Container,
} from './styles';

export default function Break({ }) {
    // const [isMenu, setIsMenu] = useState(false);

    return (
        <Container>
            Message
        </Container>
    )
}