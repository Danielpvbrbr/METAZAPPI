import {
    Container,
} from './styles';

export default function SettingsCf({ }) {
    // const [isMenu, setIsMenu] = useState(false);

    return (
        <Container>
            
        </Container>
    )
}