import {
    Container,
} from './styles';

export default function Interaction({ }) {
    // const [isMenu, setIsMenu] = useState(false);

    return (
        <Container>
            Interaction
        </Container>
    )
}